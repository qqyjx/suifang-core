import { BehaviorWithStore } from "mobx-miniprogram-bindings";
import store from "../store/index";

export default BehaviorWithStore({
    storeBindings: {
        store,
        fields: ["signData"],
        actions: ["updateSignData"]
    }
})