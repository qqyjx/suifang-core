# 智能随访系统 — 可穿戴设备体征数据采集与结构化处理平台

## 一、项目概述

本项目是"智能穿戴设备集成技术方案"的核心软件部分，实现了从可穿戴医疗设备到研究队列数据库的完整数据链路。系统通过微信小程序 BLE SDK 和 Android BLE SDK 两个客户端，对接多款具备二类医疗器械注册证资质的智能硬件设备，自动采集 11 类人体体征数据，经 JSON Schema 校验和 FHIR Resources 标准化映射后，上传至 MySQL 研究队列数据库，支撑临床随访研究的数据管理需求。

**考核指标达成情况**：已集成 2 款具备二类医疗器械注册证资质的 Veepoo 可穿戴智能硬件设备（达成 ≥2 款的要求），采集信息经结构化分析后上传至研究队列。

**两个 SDK 的分工**：

| SDK | 路径 | 平台 | 职责 |
|-----|------|------|------|
| WeChat Mini Program BLE SDK | `/home/qq/WeChat_Mini_Program_Ble_SDK` | 微信小程序（iOS/Android/鸿蒙） | BLE 设备连接、数据采集、结构化处理、HTTP 同步 |
| Android BLE SDK | `/home/qq/Android_Ble_SDK` | Android 原生应用 | BLE 设备连接、原生 Android 端数据采集 |

两者共享同一套设备协议层（Veepoo SDK）和后端数据库，采集的数据最终汇入同一个研究队列数据库。

---

## 二、支持的二类医疗器械设备

| 设备 | 型号 | 佩戴方式 | 生产厂商 | 注册证编号 | 采集指标 | 连接方式 | 集成状态 |
|------|------|---------|---------|-----------|---------|---------|---------|
| R04 蓝牙智能戒指 | VPR04-BLE | 手指/腕戴式 | 深圳维亿魄科技有限公司 | 粤械注准20232070845 | 心率、血氧饱和度、HRV 心率变异性、压力指数、睡眠跟踪 | BLE 4.2+ | ✅ 已集成 |
| AMOLED 屏幕智能手表 | VP-W680 | 腕戴式 | 深圳维亿魄科技有限公司 | 粤械注准20242211536 | 血压、体温、血糖评估、血液成分（尿酸/血脂）、身体成分（体脂BMI）、血氧 | BLE 5.0 | ✅ 已集成 |
全部设备均持有国家药品监督管理局颁发的二类医疗器械注册证，满足临床研究数据采集的合规要求。两款设备通过 Veepoo BLE SDK 接入。

---

## 三、技术架构

```
┌─────────────────────┐     ┌────────────────────────┐     ┌──────────────────────────────┐     ┌─────────────────────┐
│    可穿戴设备层       │────→│     客户端采集层         │────→│     数据清洗与结构化处理        │────→│   研究队列数据库      │
│                     │ BLE │                        │     │                              │     │                     │
│ R04 蓝牙智能戒指     │     │ 微信小程序 BLE SDK      │     │ ① JSON Schema 校验           │     │ MySQL 8.0           │
│ AMOLED 智能手表      │     │   - 57 个功能页面       │     │ ② FHIR Resources 映射        │     │ 数据库: smart_       │
│                     │     │   - TypeScript 类型校验  │     │ ③ 字段去重 / 单位标准化       │     │   followup_research  │
│                     │     │   - 本地缓存            │     │ ④ HTTPS / MQTT 实时传输       │     │ 9 张业务表           │
│                     │     │                        │     │                              │     │ 3,395+ 条记录       │
│                     │     │ Android BLE SDK         │     │ ⑤ PaddleOCR-VL 手动录入      │     │ 结构化分析报告       │
│                     │     │   - 原生 Android 连接    │     │ ⑥ Qwen3 智能解析             │     │                     │
└─────────────────────┘     └────────────────────────┘     └──────────────────────────────┘     └─────────────────────┘
```

### 各层说明

**设备层**：2 款 Veepoo 二类医疗器械通过 BLE 协议与客户端通信，使用私有协议由 `vp_sdk`（378KB 编译后 JS）封装；杰理芯片设备由 `jieli_sdk` 处理 OTA 和表盘传输。

**采集层**：微信小程序端提供 58 个功能页面，覆盖心率测量、血氧监测、ECG 测量、血糖检测、睡眠分析、数据导出等全部功能。数据采集后先写入本地文件系统缓存，再通过 HTTP 同步服务上传。Android SDK 提供等效的原生端采集能力。

**结构化处理层**：
- JSON Schema 校验：所有体征数据按 `healthData.ts` 中定义的 11 个 TypeScript 接口校验字段完整性和类型正确性
- FHIR Resources 映射：将体征数据映射为 HL7 FHIR 标准资源（Observation、DiagnosticReport），确保医疗数据互操作性
- 传输通道：支持 HTTPS（同步上传）和 MQTT（实时流式传输）两种方式
- 手动数据：支持通过 PaddleOCR-VL 识别纸质报告 + Qwen3 智能解析，处理非数字化来源的体征数据

**数据库层**：MySQL 8.0 研究队列数据库，9 张表覆盖队列管理、受试者信息、设备溯源、体征数据、同步审计、结构化报告全链路。

---

## 四、数据采集能力（11 类体征数据）

| 序号 | 数据类型 | TypeScript 接口 | 关键字段 | 单位 | 采集频率 | 数据来源设备 |
|------|---------|----------------|---------|------|---------|------------|
| 1 | 心率 | `HeartRate` | `heartRate`, `heartState` | bpm | 实时/每分钟 | R04 戒指、AMOLED 手表 |
| 2 | 血氧饱和度 | `BloodOxygen` | `spo2` | % | 每 2 小时 | R04 戒指、AMOLED 手表 |
| 3 | 血压 | `BloodPressure` | `systolic`, `diastolic`, `riskLevel` | mmHg | 早/中/晚 | AMOLED 手表 |
| 4 | 体温 | `Temperature` | `temperature`, `skinTemperature` | °C | 每 4 小时 | AMOLED 手表 |
| 5 | 心电图 (ECG) | `ECG` | `ecgData[]`, `leadType` | mV | 按需测量 | R04 戒指（单导联） |
| 6 | 血糖 | `BloodGlucose` | `bloodGlucose`, `riskLevel` | mmol/L | 餐前/餐后 | AMOLED 手表 |
| 7 | 血液成分 | `BloodLiquid` | `uricAcid`, `bloodLipid` | 多种 | 每日 | AMOLED 手表 |
| 8 | 身体成分 | `BodyComposition` | `bmi`, `bodyFat`, `muscleMass` | 多种 | 每日 | AMOLED 手表 |
| 9 | 步数 | `Step` | `stepCount`, `distance`, `calories` | 步/km/kcal | 每日汇总 | R04 戒指、AMOLED 手表 |
| 10 | 睡眠 | `Sleep` | `deepSleep`, `lightSleep`, `remSleep` | 分钟 | 每日汇总 | R04 戒指 |
| 11 | 日常综合 | `Daily` | `dailySteps`, `dailyCalories` | 多种 | 每日汇总 | 所有设备 |

所有数据类型在 `miniprogram/types/healthData.ts` 中以 TypeScript 接口严格定义，继承自 `BaseRecord`（含 ISO 8601 时间戳），确保采集数据的类型安全。

---

## 五、数据结构化处理流程

```
BLE 原始字节流
    ↓
Veepoo SDK 协议解析（vp_sdk/index.js）
    ↓
TypeScript 类型实例化（healthData.ts 接口校验）
    ↓
JSON Schema 结构化校验
    ├── 字段完整性检查（必填字段不为空）
    ├── 值域校验（心率 30-250 bpm、血氧 70-100%、血压 60-260 mmHg）
    └── 单位标准化（体温 °F→°C、血糖 mg/dL→mmol/L）
    ↓
FHIR Resources 映射
    ├── 心率/血氧/血压/体温/ECG/血糖/睡眠/步数 → FHIR Observation
    └── 结构化报告（日报/周报） → FHIR DiagnosticReport
    ↓
数据去重（同一设备同一时间戳的重复上传过滤）
    ↓
HTTP POST 上传（HTTPS 或 MQTT 通道）
    ↓
MySQL 研究队列数据库（smart_followup_research）
```

### FHIR 资源映射对照表

| 体征数据 | FHIR Resource Type | LOINC Code | Code 名称 |
|---------|-------------------|------------|----------|
| 心率 | Observation | 8867-4 | Heart rate |
| 血氧 | Observation | 2708-6 | Oxygen saturation (SpO2) |
| 血压 | Observation | 85354-9 | Blood pressure panel |
| 体温 | Observation | 8310-5 | Body temperature |
| ECG | Observation | 131328 | MDC_ECG_ELEC_POTL |
| 血糖 | Observation | 15074-8 | Glucose [Moles/volume] |
| 睡眠 | Observation | 93832-4 | Sleep duration |
| 步数 | Observation | 55423-8 | Number of steps |
| 日报/周报 | DiagnosticReport | — | 自定义 |

---

## 六、研究队列数据库

### 连接信息

| 配置项 | 值 |
|-------|---|
| 数据库类型 | MySQL 8.0 |
| 数据库名 | `smart_followup_research` |
| 用户名 | `root` |
| 密码 | `health123` |
| 主机 | `localhost` |
| 端口 | `3306` |
| 字符集 | `utf8mb4_unicode_ci` |

```bash
# 连接命令
mysql -u root -phealth123 smart_followup_research

# 启动 MySQL 服务（如未运行）
sudo service mysql start
```

### 数据库表结构（9 张表）

#### 1. research_cohorts — 研究队列（2 条记录）

管理研究课题队列，记录研究目的、负责人、目标样本量和队列状态。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT PK | 队列编号 |
| cohort_name | VARCHAR(100) | 队列名称 |
| research_purpose | TEXT | 研究目的 |
| principal_investigator | VARCHAR(50) | 负责人 |
| start_date | DATE | 启动日期 |
| status | ENUM | recruiting/active/completed/suspended |
| target_size | INT | 目标样本量 |

当前队列：
- "智能穿戴设备社区随访研究"（目标 200 人，已入组 3 人）
- "老年慢病远程监护试点"（目标 150 人，已入组 2 人）

#### 2. patients — 受试者信息（5 条记录）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT PK | 患者编号 |
| cohort_id | INT FK | 所属队列 |
| patient_no | VARCHAR(20) | 受试者编号（如 SF-2026-001） |
| name | VARCHAR(50) | 姓名 |
| gender | ENUM(M/F) | 性别 |
| age | INT | 年龄 |
| diagnosis | VARCHAR(200) | 主要诊断 |
| enrolled_at | DATE | 入组日期 |

#### 3. medical_devices — 二类医疗器械设备（8 条记录）

每个设备记录均包含医疗器械注册证编号，确保数据可追溯至合规设备。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT PK | 设备编号 |
| device_name | VARCHAR(100) | 设备名称 |
| model | VARCHAR(100) | 型号 |
| device_type | ENUM | wrist/head/chest/finger |
| manufacturer | VARCHAR(100) | 生产厂商 |
| registration_cert_no | VARCHAR(100) | **二类医疗器械注册证编号** |
| mac_address | VARCHAR(20) | BLE MAC 地址 |
| patient_id | INT FK | 绑定患者 |

#### 4. vital_heart_rate — 心率监测数据（1,680 条记录）

5 名受试者 × 14 天 × 24 次/天 = 1,680 条，模拟每小时一次的连续心率监测。

| 字段 | 类型 | 说明 |
|------|------|------|
| patient_id | INT FK | 患者 |
| device_id | INT FK | 采集设备 |
| heart_rate | INT | 心率 (bpm) |
| heart_state | ENUM | resting/active/sleeping/exercise |
| recorded_at | DATETIME | 采集时间 |

#### 5. vital_blood_oxygen — 血氧监测数据（840 条记录）

每 2 小时采集一次，12 次/天 × 14 天 × 5 人。

| 字段 | 类型 | 说明 |
|------|------|------|
| spo2 | DECIMAL(4,1) | 血氧饱和度 (%) |

#### 6. vital_blood_pressure — 血压监测数据（210 条记录）

早/中/晚各一次，3 次/天 × 14 天 × 5 人。

| 字段 | 类型 | 说明 |
|------|------|------|
| systolic | INT | 收缩压 (mmHg) |
| diastolic | INT | 舒张压 (mmHg) |
| pulse_rate | INT | 脉率 |
| risk_level | ENUM | normal/elevated/hypertension_1/hypertension_2/crisis |

#### 7. vital_signs — 综合体征数据 JSON（445 条记录）

体温、血糖、睡眠、步数、身体成分等数据统一存储为 JSON 格式，对应 JSON Schema 校验流程。

| 字段 | 类型 | 说明 |
|------|------|------|
| data_type | ENUM | temperature/blood_glucose/sleep/step/ecg/blood_component/body_composition |
| vital_data | JSON | 结构化体征数据（经 JSON Schema 校验） |
| fhir_resource_type | VARCHAR(50) | FHIR 资源类型映射（Observation） |

#### 8. data_sync_logs — 数据同步与校验日志（140 条记录）

完整记录每次数据同步事件，包含传输通道和 FHIR 结构化校验结果。

| 字段 | 类型 | 说明 |
|------|------|------|
| sync_channel | ENUM | BLE_HTTPS / BLE_MQTT / MANUAL_OCR |
| records_count | INT | 同步记录数 |
| fhir_validated | BOOLEAN | FHIR 结构化校验是否通过 |
| json_schema_valid | BOOLEAN | JSON Schema 校验是否通过 |
| status | ENUM | success/partial/failed |

#### 9. structured_reports — 结构化分析报告（80 条记录）

自动生成的日报/周报，包含趋势分析和风险标记，映射为 FHIR DiagnosticReport。

| 字段 | 类型 | 说明 |
|------|------|------|
| report_type | ENUM | daily/weekly/monthly/alert |
| data_summary | JSON | 结构化摘要（平均心率、血氧、血压、步数、睡眠评分等） |
| fhir_resource_type | VARCHAR(50) | DiagnosticReport |
| risk_flags | JSON | 风险标记（高血压预警、低血氧预警等） |

### 常用查询命令

**查询 1：研究队列总览**
```sql
SELECT c.cohort_name AS '队列名称', c.principal_investigator AS '负责人',
       c.status AS '状态', c.target_size AS '目标样本量',
       COUNT(DISTINCT p.id) AS '已入组受试者',
       COUNT(DISTINCT d.id) AS '绑定设备数'
FROM research_cohorts c
LEFT JOIN patients p ON p.cohort_id = c.id
LEFT JOIN medical_devices d ON d.patient_id = p.id
GROUP BY c.id;
```

**查询 2：二类医疗器械设备清单**
```sql
SELECT d.device_name AS '设备名称', d.model AS '型号',
       d.device_type AS '类型', d.registration_cert_no AS '医疗器械注册证编号',
       d.mac_address AS 'MAC地址', p.name AS '绑定患者'
FROM medical_devices d
JOIN patients p ON d.patient_id = p.id
ORDER BY d.device_name;
```

**查询 3：受试者心率趋势（14 天）**
```sql
SELECT DATE(recorded_at) AS '日期', COUNT(*) AS '采集次数',
       ROUND(AVG(heart_rate),1) AS '平均心率',
       MIN(heart_rate) AS '最低', MAX(heart_rate) AS '最高'
FROM vital_heart_rate WHERE patient_id = 1
GROUP BY DATE(recorded_at) ORDER BY 日期;
```

**查询 4：数据同步日志（含 FHIR 校验状态）**
```sql
SELECT p.name AS '患者', d.device_name AS '设备',
       sl.sync_channel AS '通道', sl.records_count AS '记录数',
       IF(sl.fhir_validated,'通过','未通过') AS 'FHIR校验',
       sl.status AS '状态',
       DATE_FORMAT(sl.synced_at,'%m-%d %H:%i') AS '时间'
FROM data_sync_logs sl
JOIN patients p ON sl.patient_id = p.id
JOIN medical_devices d ON sl.device_id = d.id
ORDER BY sl.synced_at DESC LIMIT 10;
```

**查询 5：数据库总量统计**
```sql
SELECT
  (SELECT COUNT(*) FROM research_cohorts) AS '研究队列',
  (SELECT COUNT(*) FROM patients) AS '受试者',
  (SELECT COUNT(*) FROM medical_devices) AS '设备',
  (SELECT COUNT(*) FROM vital_heart_rate) +
  (SELECT COUNT(*) FROM vital_blood_oxygen) +
  (SELECT COUNT(*) FROM vital_blood_pressure) +
  (SELECT COUNT(*) FROM vital_signs) AS '体征记录总数',
  (SELECT COUNT(*) FROM data_sync_logs) AS '同步日志',
  (SELECT COUNT(*) FROM structured_reports) AS '分析报告';
```

---

## 七、微信小程序 SDK 使用指南

### 环境要求

| 工具 | 版本要求 | 用途 |
|------|---------|------|
| 微信开发者工具 | 最新稳定版 | 小程序编译、调试、预览 |
| Node.js | ≥ 16.x | 数据同步服务端 |
| MySQL | 8.0+ | 研究队列数据库 |
| 支持 BLE 的手机 | Android 5.0+ / iOS 10+ | 真机 BLE 测试 |

### 安装与运行

```bash
# 1. 进入项目目录
cd /home/qq/WeChat_Mini_Program_Ble_SDK

# 2. 在微信开发者工具中导入项目
#    项目路径：mini_program_code/demo/WeiXinSDKTSDemo
#    AppID：wxbc5453a4c53dbee8

# 3. 开发者工具设置
#    勾选"不校验合法域名"（本地开发必须）
#    勾选"增强编译"
#    基础库版本 ≥ 3.8.9

# 4. 启动数据同步服务端
cd scripts && npm install && npm start  # 端口 3000

# 5. 编译运行 → 手机扫码预览（需开启蓝牙和位置权限）
```

### 使用流程

```
打开小程序 → 设备扫描 → 选择设备连接 → 密钥认证 → 进入功能页面（58个）
                                                          ↓
                 ← 数据自动同步至研究队列数据库 ← 采集体征数据 ←┘
```

### 项目目录结构

```
WeChat_Mini_Program_Ble_SDK/
├── mini_program_code/demo/WeiXinSDKTSDemo/     # 微信小程序主项目
│   └── miniprogram/
│       ├── pages/                 # 58 个功能页面
│       │   ├── bleConnection/     # BLE 设备扫描与连接（入口页）
│       │   ├── heartRateTest/     # 心率实时测量
│       │   ├── bloodOxygen/       # 血氧监测
│       │   ├── bloodGlucose/      # 血糖监测
│       │   ├── bodyTemperature/   # 体温监测
│       │   ├── ecgTest/           # ECG 心电图
│       │   ├── sleep/             # 睡眠监测
│       │   ├── step/              # 计步
│       │   ├── dataManagement/    # 数据导出管理
│       │   └── ...
│       ├── services/
│       │   ├── dataStorage.ts     # 本地数据存储服务（单例模式）
│       │   └── httpSync.ts        # HTTP 数据同步服务
│       └── types/
│           └── healthData.ts      # 11 种体征数据类型定义
├── libs/
│   ├── vp_sdk/                    # Veepoo BLE SDK（378KB）
│   └── jieli_sdk/                 # 杰理 BLE SDK（OTA/表盘）
├── scripts/
│   └── health-data-server.js      # Node.js 数据同步服务端
├── database/
│   ├── schema.sql                 # 数据库表结构
│   └── init.sql                   # 完整数据库（含演示数据）
└── docs/
    └── VeepooWeiXinSDK使用文档.md # SDK API 文档（5600+ 行）
```

### BLE 连接与数据采集代码示例

```typescript
import VPManager from '../../libs/vp_sdk/index';

// 1. 扫描 BLE 设备
VPManager.startScanDevice((device) => {
  console.log('发现设备:', device.name, device.mac);
});

// 2. 连接设备（通过 MAC 地址）
VPManager.connectDevice(macAddress, (state) => {
  if (state === 'connected') {
    // 3. 密钥认证（必须在采集数据前完成）
    VPManager.sendSecretKey(key, (result) => {
      console.log('认证成功，可以开始采集数据');
    });
  }
});

// 4. 采集心率数据
VPManager.startDetectingHeartRate((data) => {
  const record: HeartRate = {
    timestamp: new Date().toISOString(),
    heartRate: data.heartRate,
    heartState: data.state  // resting/active/sleeping/exercise
  };
  // 保存到本地并自动同步到服务端
  DataStorageService.getInstance().saveHeartRate(record);
});

// 5. 采集血氧数据
VPManager.startDetectingBloodOxygen((data) => {
  const record: BloodOxygen = {
    timestamp: new Date().toISOString(),
    spo2: data.bloodOxygen
  };
  DataStorageService.getInstance().saveBloodOxygen(record);
});
```

---

## 八、SDK 版本历史

| 版本 | 日期 | 编辑 | 更新内容 |
|------|------|------|---------|
| V1.1.16 | 2026/01/09 | 陈显文 | 新增配置 4G 服务信息接口 |
| V1.1.15 | 2026/01/04 | 陈显文 | 修复已知问题 |
| V1.1.14 | 2025/12/29 | 陈显文 | 新增 ZT163 常灭屏功能接口 |
| V1.1.13 | 2025/12/16 | 陈显文 | 新增 JH58 动态血压监测、血液成分接口更新 |
| V1.1.12 | 2025/12/05 | 陈显文 | 血糖风险等级解析优化 |
| V1.1.11 | 2025/11/19 | 陈显文 | ECG 波形兼容性增强 |
| V1.1.9 | 2025/10/22 | 陈显文 | BLE 扫描修复 |
| V1.1.8 | 2025/07/10 | 陈显文 | 新增多个数据采集接口 |
| V1.1.7 | 2025/03/22 | 陈显文 | 初始稳定版本 |

### 运行环境兼容性

| 平台 | 支持状态 | 备注 |
|------|---------|------|
| Android | ✅ 完全支持 | Android 5.0+ |
| iOS | ✅ 完全支持 | iOS 10+ |
| HarmonyOS 4.0 | ✅ 支持 | 兼容 Android BLE API |
| HarmonyOS Next | ⚠️ 部分支持 | 部分功能需适配 |

---

## 九、注意事项

1. **蓝牙测试需真机**：微信开发者工具模拟器不支持 BLE 通信，所有设备功能必须在真机上测试
2. **密钥认证**：连接设备后必须完成密钥认证才能调用数据采集 API，否则会返回空数据
3. **框架限制**：微信小程序 SDK 仅支持原生 + TypeScript 开发，不兼容 mpvue、uni-app、Taro 等框架
4. **数据合规**：采集的体征数据涉及个人健康信息，使用时需遵守《个人信息保护法》和《医疗数据安全管理办法》
5. **设备电量**：长时间连续采集（如 24 小时心率监测）会显著消耗设备电量，建议设置合理的采集间隔
6. **数据库备份**：研究队列数据库应定期备份，`database/init.sql` 可用于完整恢复
